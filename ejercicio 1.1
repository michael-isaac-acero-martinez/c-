// ejercicio 1.1
#include <iostream>

using namespace std;

int main(){
    
    int n1, n2, sum, res, mul, divi;
    
    cout<<"Ingrese los numeros a realizar: ";cin>>n1>>n2;
    
    sum = (n1 + n2), res = (n1 - n2), mul = (n1 * n2), divi = (n1 / n2);
    
    cout<<"Los resultados son";
    
    cout<<"\nLa suma es: "<<sum;
    cout<<"\nLa resta es: "<<res;
    cout<<"\nLa multiplicacion es: "<<mul;
    cout<<"\nLa division es: "<<divi;
    
    
    return 0;
}

michael-isaac-acero-martinez
