// ejercicio 2.3
#include <iostream>

using namespace std;

int main() {
    double a, b, c, d, e, f, num, den, res = 0;

    cout<<"ingrese los valores de a, b, y c :"; cin>>a>>b>>c;
    cout<<"ingrese los valores de d, e, y f :"; cin>>d>>e>>f;

    num = a+(b/c);
    den = d+(e/f);
    res = num / den;
    
    cout<<"el resultado es :"<<res;

}